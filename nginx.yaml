services:
  webserver:
    image: nginx:alpine
    container_name: 68-s2-fouru-webserver
    restart: unless-stopped
    depends_on:
      - s2app
    ports:
      - "${NGINX_PORT:-80}:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
