services:
  webserver:
    image: nginx:alpine
    container_name: 68-s2-webserver
    restart: unless-stopped
    ports:
      - "${NGINX_PORT:-80}:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
